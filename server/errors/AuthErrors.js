const INVALID_FIELDS =
  "Email, name, or password cannot be empty. Passwords cannot be less than 8 characters";
const ACCOUNT_EXISTS = "An account with that email already exists";
const USER_NOT_FOUND = "User with that email was not found";
const INVALID_CREDENTIALS = "Invalid credentials";
const MISSING_TOKEN = "Missing token";
module.exports = {
  INVALID_CREDENTIALS,
  INVALID_FIELDS,
  USER_NOT_FOUND,
  ACCOUNT_EXISTS,
  MISSING_TOKEN,
};
